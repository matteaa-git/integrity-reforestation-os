# Integrity Social Media Machine

Content production and paid-growth operating system for Integrity Reforestation.

## Project Structure

- `apps/web` — Next.js 14 + TypeScript (pnpm)
- `apps/api` — FastAPI + Python 3.11+ (venv)
- `apps/worker` — Python 3.11+ worker service (venv)
- `packages/*` — Shared TypeScript packages (pnpm workspace)

## Run Instructions

See README.md for full setup and run commands.

## Rules

- Read docs before coding.
- Keep the repo runnable at all times.
- Document decisions in `docs/05-decisions.md`.
- Scaffold only — no product logic until approved.
